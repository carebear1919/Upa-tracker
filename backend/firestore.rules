rules_version = '2';

// Single-family app: any authenticated member of the family account can
// read/write everything. No public access, no per-tenant accounts.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
